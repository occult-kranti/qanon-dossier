# Q-drops clustering pipeline — dependencies
#
#   pip install -r requirements.txt
#
# --method kmeans needs only scikit-learn (no torch / no model download).
# --method bertopic / hdbscan need the embedding + density-clustering stack.
# gensim is used for c_v topic coherence; skip it with --no-coherence if you prefer.

# --- core (all methods) ---
scikit-learn>=1.3
numpy>=1.24

# --- embeddings + density clustering (bertopic / hdbscan) ---
bertopic>=0.16            # embeddings + UMAP + HDBSCAN + c-TF-IDF labelling
sentence-transformers>=2.2
umap-learn>=0.5
hdbscan>=0.8

# --- topic-coherence validity score (optional) ---
gensim>=4.3               # c_v coherence; omit if you always run --no-coherence

# sentence-transformers pulls in torch automatically. On Apple Silicon or for a
# CPU-only build, install torch first per https://pytorch.org/get-started/.
